import 'package:flutter/material.dart';
import 'package:shape_platform/main.dart';

class getUser extends StatelessWidget {
  //const User({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: TextWidget(),
        ),
      ),
    );
  }
}
